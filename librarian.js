const librarian = Object.create({}, {
    newCustomer: {
        value: function (firstName, lastName, libraryNum, ) {
            
            const newCustomer = {
                'firstName': firstName,
                'lastName': lastName,
                'cardNumber': cardNumber
                
            }
         console.log(newCustomer);

        
            
        }
    }
})

